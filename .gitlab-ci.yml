pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp ui/gunnerus.html public/index.html
    - cp ui/api_view.html public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
