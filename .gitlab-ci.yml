pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp ui/gunnerus.html public/gunnerus.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
